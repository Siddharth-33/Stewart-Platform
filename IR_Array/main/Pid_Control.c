#include <stdio.h>
#include "IR_Array.c"

 

// void Pid_Control(void){
// float kp=1.0,ki= 0,kd=0.1, dt=0.001;
// float error_x,error_y,integral_x,integral_y,old_error_x,old_error_y;

// while(1){
//     float compute_pid(float error, float integral, float old_error) {
//     float derivative = (error - old_error)/dt;
//      integral = integral + ki*error*dt;
//      old_error = error;
//     return kp * error + (integral) + kd * derivative;
// };
//     error_x = 0 - Xval;
//     error_y = 0 - Yval;

//     float output_x = compute_pid(error_x, integral_x, old_error_x);
//     float output_y = compute_pid(error_y, integral_y, old error_y);
//     printf("o/p x:-%f  o/p y:-%f ",output_x,output_y);

//    }

// }

